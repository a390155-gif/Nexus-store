# Despliegue automático desde GitHub en Render
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: nexus-store-a390155
    runtime: node
    plan: free
    repo: https://github.com/a390155-gif/Nexus-store
    branch: main
    rootDir: ecommerce/backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: MONGODB_URI
        value: mongodb+srv://a390155_db_user:224426628816Db@cluster0.o9a3opl.mongodb.net/?appName=Cluster0
